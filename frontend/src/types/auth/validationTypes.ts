export interface ValidationType {
  email?: boolean;
  nickname?: boolean;
  description?: boolean;
  password?: boolean;
  newPassword?: boolean;
  confirmPassword?: boolean;
  currentPassword?: boolean;
}
