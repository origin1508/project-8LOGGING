import React from "react";
import styled from "styled-components";

const ChannelEnter = () => {
  return <ChannelContainer></ChannelContainer>;
};

const ChannelContainer = styled.div`
  width: 100%;
`;

export default ChannelEnter;
